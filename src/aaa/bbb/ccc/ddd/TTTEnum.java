package aaa.bbb.ccc.ddd;

// navigation test: Ctrl-Click on FOO erc should navigate to constructor, not show usages
public enum TTTEnum {
    FOO('a'),
    ZOO('b'),
    BAR('c');

    private TTTEnum(char a) {
    }
}